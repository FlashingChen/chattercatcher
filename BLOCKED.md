# BLOCKED.md（待裁决清单）

## 已知局限（不阻塞本次交付）
- XLSX 解析对日期单元格只输出序列号原值（如 `45308`），未按 styles.xml 数字格式转成可读日期。当前实现只解析共享字符串/内联字符串/数值等 `<v>` 文本。若需日期可读，需解析 `xl/styles.xml` 的数字格式并转换——是否投入待裁决。
- HTML 解析不抓取聊天消息里的 URL（领导拍板：出网请求有隐私风险，本次只做 .html/.htm 本地文件解析，链接抓取待裁决）。
- Web UI 与 CLI 帮助里的「支持格式」文案按领导拍板本次未改，仅 parser、测试与文档更新。

## 环境备注（非代码问题）
- 安装依赖期间 registry.npmjs.org 网络不稳定（ECONNRESET/ETIMEDOUT），已通过提高 fetch 重试次数完成安装；exceljs 因依赖面太大且网络不稳改用 jszip+cheerio 手写解析，理由已记入 PROGRESS.md。

## 无阻塞性待裁决项
- 无。三项新格式解析均已完成并通过反向验证。
